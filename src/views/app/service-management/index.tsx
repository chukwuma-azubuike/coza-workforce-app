import React from 'react';
import ViewWrapper from '../../../components/layout/viewWrapper';
import { Text } from 'native-base';

const ServiceManagement: React.FC = () => {
    return (
        <ViewWrapper>
            <Text>ServiceManagement</Text>
        </ViewWrapper>
    );
};

export default ServiceManagement;
